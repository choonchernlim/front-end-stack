// @flow
import React, { Element } from 'react';
import Container from '../../common/components/Container';

const Home = (): Element<any> => (
  <Container>
    <h1>Welcome!</h1>

    <p>To learn more about this project template, visit the GitHub page.</p>

  </Container>
);

export default Home;
